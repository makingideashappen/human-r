# General ignores
*.swp             # Swap files
*.DS_Store        # macOS system files
Thumbs.db         # Windows system files
*.log             # Log files

# Ignore environment files
.env              # Root environment variable file, if any

# Django-specific ignores in the `api` folder
__pycache__/
*.py[cod]
*.pyo
*.pyd
db.sqlite3          # SQLite database file
media/              # Django media files, if applicable
staticfiles/        # Django collected static files
*.pot               # Translation template files
*.mo                # Compiled translation files
.env                # Environment variable file specific to Django
venv/               # Virtual environment for Django, if inside `api`
env/

# Svelte-specific ignores in the `client` folder
client/node_modules/    # Node.js dependencies
client/build/           # Svelte build output
client/.svelte-kit/     # SvelteKit output
client/package-lock.json # npm lock file
client/yarn.lock        # Yarn lock file
client/dist/            # Build directory if using bundlers
client.cache/           # Cache directory if using a bundler
client/.env             # Environment variable file for Svelte

# Coverage and testing files
coverage/
*.coverage

# Ignore IDE and editor config files
.vscode/
.idea/
*.sublime-workspace
*.sublime-project